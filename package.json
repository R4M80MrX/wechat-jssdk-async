{
  "name": "wechat-jssdk-async",
  "version": "0.0.1",
  "main": "lib/index.js",
  "license": "MIT",
  "files": [
    "lib"
  ],
  "source": true,
  "scripts": {
    "build": "yarn build:lib",
    "build:lib": "yarn clean && yarn tsc",
    "clean": "rimraf lib",
    "prepublish": "yarn build"
  },
  "dependencies": {
    "@rambox/weixin-js-sdk": "^1.6.1",
    "ts-node": "^8.8.2",
    "typescript": "^3.8.3"
  }
}
